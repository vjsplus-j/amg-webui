---
description: VUE3-AMG-WEBUI monorepo root layout and package boundaries
alwaysApply: true
---

# Project root (locked)

```
VUE3-AMG-WEBUI/
├── build/                 # 打包：全量 / 按需 / 多主题 / 类型生成
├── docs/                  # 对外官方文档站（VitePress，可部署）
├── packages/
│   ├── components/        # UI 按包拆分：core/form/data/overlay + charts/editor + media/gb28181/onvif + business
│   ├── telemetry/         # Vp Telemetry 交互观测内核（默认关闭）
│   ├── security/          # 安全防护层（消毒 / 协议拦截 / 表单过滤）
│   ├── lowcode/           # 低代码 Schema 引擎 + ui/（SchemaRenderer / Canvas*）
│   ├── runtime/           # UI Overlay 统一内核（栈 / Focus / ScrollLock / Escape）
│   ├── skill/             # 可选 Skill Runtime（Core + Vue；不进根入口）
│   ├── hooks/             # 全局组合式 API（useTheme / useLocale / useTrackedEmit 等）
│   ├── locale/            # 多语言包
│   ├── theme/             # 亮色 / 暗色 / designmd 多主题
│   ├── icons/             # SVG / Lucide 图标
│   ├── animations/        # 全局过渡动画
│   ├── utils/
│   ├── constants/
│   ├── types/
│   └── index.ts           # 库统一入口（foundation only；行业 opt-in）
├── example/                  # 本地开发调试工具（不上线；非对外 demo）
├── scripts/               # 长期工程脚本（create / entry / i18n / catalog / boundaries …）
├── tests/unit/ · tests/e2e/
├── dist/                  # 库发包产物
└── 根配置：vite / vitest / vitepress / tsconfig / package.json …
```

包说明：`packages/README.md` · Overlay：`docs/OVERLAY.md` · Skill：`packages/skill/README.md` / `docs/SKILL_RUNTIME.md` / `vue3-amg-webui-skill-runtime.mdc` · 遥测：`packages/telemetry/README.md` · example：`example/README.md`。

# Component isolation (mandatory)

- **Foundation**：`@amg-webui/core` · `form` · `data` · `overlay`（目录 `packages/components/{core,form,data,overlay}/`）。**禁止** import 行业包。
- **Industry（opt-in）**：`@amg-webui/media` · `gb28181` · `onvif`。可依赖 foundation；**不得**进入 core / 根 barrel。
- **Specialized**：`@amg-webui/charts` · `editor`；lowcode UI 在 `packages/lowcode/ui/`。
- **Business**（`packages/components/business/`）：五大域 — `login` / `users` / `orders` / `content` / `settings`。只消费 foundation + hooks + theme + utils。
- **Skill**（`packages/skill/`）：独立可选逻辑运行时；Core 不依赖 Vue 组件或 Telemetry。
- **Legacy**：`@amg-webui/components/base` **仅再导出 core**（硬切，无胖 shim）。
- 归属 SSOT：`scripts/component-package-map.mjs`；门禁：`npm run check:boundaries`。
- 路径别名：`@amg-webui/core` 等 → `packages/components/<pkg>`；业务内用 `@amg-webui/core|form|data|overlay`，不要相对路径穿包。
- Skill 公开入口只允许 `amg-webui/skill` 与 `amg-webui/skill/core`；根入口 **禁止再导出 Skill**。
- **双向隔离**：组件源码禁止 import Skill；Skill Core 禁止 import components / telemetry。
- 新增组件：`npm run create:component -- <core|form|data|overlay|charts|editor|media|gb28181|onvif|business> Name`。
- 主题只来自 `packages/theme`（六套锁定：mercedes / linear / porsche / lamborghini / ferrari / apple）。
- **example = 本地调试**（不上线）；**docs = 对外文档**。
- **禁止遗留层**：勿恢复扁平 `src/`、根级 `demo/`、本地 `backup/`。
- 构建产物：`dist/` · docs VitePress · `example-dist/`（**不上线**）；**不入库**。
