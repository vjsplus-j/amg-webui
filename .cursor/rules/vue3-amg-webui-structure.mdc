---
description: VUE3-AMG-WEBUI monorepo root layout and package boundaries
alwaysApply: true
---

# Project root (locked)

```
VUE3-AMG-WEBUI/
├── build/                 # 打包：全量 / 按需 / 多主题 / 类型生成
├── docs/                  # 对外官方文档站（VitePress，可部署）
├── packages/
│   ├── components/        # 核心组件库（base + business 隔离）
│   ├── telemetry/         # Vp Telemetry 交互观测内核（默认关闭）
│   ├── hooks/             # 全局组合式 API（useTheme / useLocale / useTrackedEmit 等）
│   ├── locale/            # 多语言包
│   ├── theme/             # 亮色 / 暗色 / designmd 多主题
│   ├── icons/             # SVG / Lucide 图标
│   ├── animations/        # 全局过渡动画
│   ├── utils/
│   ├── constants/
│   ├── types/
│   └── index.ts           # 库统一入口
├── example/                  # 本地开发调试工具（不上线；非对外 demo）
├── scripts/               # 自动化（新建组件、入口生成、多语言提取、catalog …）
├── tests/unit/ · tests/e2e/
├── dist/                  # 库发包产物
└── 根配置：vite / vitest / vitepress / tsconfig / package.json …
```

包说明：`packages/README.md` · 遥测：`packages/telemetry/README.md` · example：`example/README.md`。
# Component isolation (mandatory)

- **Base** (`packages/components/base/`): 纯 UI 原语（Button、Input、Dialog…）。**禁止**依赖 business。
- **Business** (`packages/components/business/`): 五大业务复合模块 — `login` / `users` / `orders` / `content` / `settings`。只消费 base + hooks + theme + utils。
- 路径别名：`@amg-webui/*` → `packages/*`；业务内用 `@amg-webui/components/base`，不要相对路径穿到 base 源码深处。
- 新增基础组件放 `base/`；新增域模块放 `business/<domain>/`（`index.ts` · `*.vue` · `types.ts` · `composables/` · `style.scss`）。
- 主题只来自 `packages/theme`（六套锁定：mercedes / linear / porsche / lamborghini / ferrari / apple）。
- **example = 本地调试**（不上线）：七大专区 `base|biz|theme|i18n|perf|lab|dev` + `overview`；压测 / bug 复现只放 example。见 `docs/APP_WORKFLOW.md` §0。
- **docs = 对外文档**：标准示例 / API / 教程；禁止压测与 bug 复现页。
- **调试页与路由只在 `example/`**：`pages/` + `router/routes.ts` + `layouts/`。
- **禁止遗留层**：勿恢复扁平 `src/` 再导出、根级 `demo/`、本地 `backup/` / 预览截图。
- 构建产物：`dist/`（发包）· docs VitePress（可部署）· `example-dist/`（example 本地冒烟，**不上线**）；**不入库**。
