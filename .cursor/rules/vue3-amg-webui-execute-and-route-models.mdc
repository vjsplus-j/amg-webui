---
description: 自行定序执行；按阶段选用不同模型，禁止全程依赖 Auto
alwaysApply: true
---

# Execute + model routing（locked）

## Execute first
收到可执行任务后：**分析 → 自己定顺序 → 直接推进**。不要把优先级 / A-B 选择题甩回用户。
仅在缺关键信息、不可逆高风险、或现有材料无法判断时提问；提问只问一个点，并写明「不回复则按 X 继续」。

## Model routing（强制偏好）
**不要**把整条链路都交给 Cursor 自带 **Auto** 糊弄完。
不同阶段应使用不同能力档位的模型，以提高质量与性价比：

| 阶段 | 目标 | 模型取向 |
|------|------|----------|
| 扫上下文 / 找文件 / 定位符号 | 快、广、低成本 | **快模型**（探索 / 检索） |
| 读关键路径、理清依赖与契约 | 准确、少幻觉 | **强阅读 / 中强推理** |
| 架构取舍、根因、跨模块设计 | 深度思考 | **高推理模型**（勿用最快档硬撑） |
| 写/改代码、补类型、修编译 | 落地、跟仓库风格 | **代码向模型**（实现档） |
| 最终核对 / 难 bug / 安全敏感 | 二次把关 | **更强模型复核**（可短回合） |

## Do
- 阶段切换时主动换档：探索完再升到深思；方案定了再切到写码；写完若风险高再切复核。
- 派子代理 / Task 时按子任务选模型，**禁止**一律 `inherit` / 一律 Auto。
- 一句话说明当前阶段与选用取向（例如「先用快模型扫仓库，再用代码模型改 `packages/`」），然后继续执行，不等用户排期。

## Don’t
- 「全程 Auto」当作默认最优。
- 用最快模型做架构决策；用最重模型做全库无脑 grep。
- 分析完甩菜单让用户选模型/优先级（应自行定默认档并执行；用户已指定模型则服从）。

## Ask mode
Ask：只给结论与推荐模型路由；提醒切 Agent 后再改代码。不要变成「你选哪个模型？」问卷——直接给出推荐默认并说明原因。
