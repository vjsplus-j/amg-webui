UI组件库 Button 完整版需求清单（企业级生产可用·最终规范）

## 一、组件基础规范（强制统一）

1. 组件前缀统一：**a-button**，全局命名统一，无差异化前缀

2. 框架适配：支持Vue3 / React双框架适配（根据组件库技术栈适配），TS全量类型导出，类型严谨无隐式任意类型

3. 兼容性：消除移动端300ms点击延迟，适配PC/移动端多端场景

4. 国际化：内置i18n插槽与文案配置，支持全局/单组件自定义多语言文案

5. 主题适配：全量色值、尺寸、圆角、阴影、动效接入全局主题变量，支持自定义主题、深色/浅色模式自动切换

6. 无障碍规范：遵循a11y标准，内置ARIA属性、键盘Tab聚焦、回车/空格触发点击、聚焦边框样式

## 二、样式变体体系（全覆盖业务场景）

完整支持 **6大基础类型 × 4种样式形态**，覆盖所有中后台、C端业务按钮场景

### 1. 基础类型（固定枚举）

default、primary、success、warning、danger、link

### 2. 样式形态（全覆盖）

- 实心按钮（默认）：常规填充样式，主操作场景专用

- 描边按钮（outline）：仅边框透明背景，次要操作场景专用

- 虚线按钮（dashed）：虚线边框，新增、占位、自定义操作场景专用

- 文本按钮（text）：无边框无背景，纯文字操作，页面轻交互场景

### 3. 特殊形态按钮

- 圆形按钮：纯图标场景圆形样式

- 圆角自定义：支持传入数值/百分比，全局默认圆角可配置

- 块级按钮：宽度100%自适应父容器，表单底部提交栏专用

- 行内按钮：自适应内容宽度，常规行内操作

## 三、尺寸规范（统一5档尺寸）

固定五档尺寸，全局统一间距、字号、内边距，支持全局默认配置

xs（超小）、sm（小）、md（默认）、lg（大）、xl（超大）

适配场景：表格行小操作、弹窗底部按钮、页面主按钮、顶部大功能按钮等全场景适配

## 四、状态体系（完整交互状态闭环）

### 1. 基础状态

- 正常状态：默认展示、hover悬浮、active点击、focus聚焦完整样式

- 禁用状态：置灰、禁止点击、禁止聚焦，支持禁用hover提示文案

- 只读状态：样式正常、禁止点击，区别于禁用状态

### 2. Loading加载状态（企业级防抖防重核心）

- 内置loading动画，支持开启/关闭

- 支持自定义loading图标、自定义加载文案

- 内置**防抖+节流双模式**，可配置时长

- 高频点击防重：加载期间禁止重复触发点击事件，解决表单重复提交、批量重复操作问题

- 加载状态自动锁定按钮交互，结束后自动恢复

## 五、图标与内容布局能力

1. 支持图标+文字组合、纯图标、纯文字三种展示模式

2. 图标位置可配置：前置、后置、上下居中布局

3. 支持自定义图标大小、图标与文字间距

4. 开放默认插槽、前置插槽、后置插槽，支持高度自定义内部内容

## 六、业务高阶能力（核心商用刚需）

### 1. 权限控制系统

- 权限隐藏：无权限直接销毁组件，不渲染DOM

- 权限置灰：无权限保留DOM，置灰禁用，保留页面布局

- 权限提示：无权限hover气泡提示、点击弹窗提示自定义文案

### 2. 操作安全能力

- 内置二次确认：删除、清空、批量操作高危场景，支持绑定内置确认弹窗，可配置确认文案、取消/确认回调

- 点击前置拦截钩子：支持全局/单按钮前置校验、埋点上报、权限校验、参数校验

- 支持原生表单能力：submit提交、reset重置原生事件适配

### 3. 路由跳转能力

- link类型支持路由跳转、外链跳转

- 支持新窗口打开、当前页跳转、携带路由参数

## 七、组合组件能力（配套完整）

1. ButtonGroup 按钮组组件

- 多个按钮合并展示，自动合并边框、消除重复间距

- 支持尺寸、类型统一继承，支持单独修改组内单个按钮状态

- 支持分段按钮、切换按钮组场景

2. 联动下拉按钮

- 内置dropdown联动能力，支持主按钮操作+下拉拓展操作组合

## 八、全局配置与自定义能力

1. 全局默认配置：统一默认尺寸、默认类型、默认防抖时长、默认圆角、loading样式

2. 样式自定义：支持自定义hover/active/focus独立样式、自定义边框色、背景色、文字色

3. 动效配置：支持水波纹点击动效开关、动效时长自定义

4. 光标样式自定义：常规指针、禁用光标、加载光标差异化展示

## 九、工程化与文档要求

1. 全属性TS类型定义完整，支持代码提示

2. 所有API、属性、事件、插槽、样式变量、全局配置完整文档说明

3. 覆盖所有业务场景demo：表单提交、弹窗确认、批量删除、权限按钮、加载防抖、按钮组、二次确认

4. 提供常见问题FAQ、踩坑解决方案、性能优化说明

## 十、最终覆盖业务场景

完全覆盖：表单提交、弹窗确认/取消、页面新增/编辑/删除、批量处理、表格行操作、顶部功能按钮、高危操作二次确认、权限受控按钮、路由跳转按钮、表单重置、极简文本操作等**全部企业级中后台业务场景**

## 十一、合规收尾

补齐原有缺失：outline/虚线/文本按钮、按钮组、权限细化、二次确认、自定义loading、节流防抖双模式、a11y无障碍、全局主题配置、插槽自定义、路由跳转、前置拦截钩子、深色模式适配