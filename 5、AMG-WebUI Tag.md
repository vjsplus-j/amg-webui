AMG-WebUI Tag 标签组件 - 完整规范与需求清单
文档归属：AMG-WebUI 组件库
组件名称：a-tag
适配版本：Vue3 / React 通用
规范等级：企业级生产可用，与 Button / Icon / Link / Typography 体系完全统一
一、基础统一规范（AMG-WebUI 强制标准）
1. 组件命名：统一前缀 a-tag，全局体系统一，无自定义差异化前缀
2. 技术标准：全量 TS 强类型约束、无任意类型、支持 Tree-Shaking 按需引入
3. 兼容性处理：适配 PC / 移动端全场景，消除不同端标签圆角、边距、渲染偏差
4. 主题体系：颜色、尺寸、圆角、间距全部取自全局主题 Token，支持浅色/深色模式自动切换适配
5. 国际化支持：文案完全兼容全局 i18n，适配多语言切换，无文案错乱、截断问题
6. 样式隔离：自带组件样式隔离，杜绝全局 CSS 污染、样式穿透覆盖问题
7. 无障碍规范：遵循 a11y 标准，语义化标签、屏幕阅读器精准识别、聚焦状态合规
二、组件定位与业务场景
组件定位：轻量状态标记、分类展示组件，用于页面简短状态标注、数据分类、属性标记，低视觉权重、高辨识度，不抢占主体内容层级。
核心业务场景（用户指定）：
- 订单状态标记：待支付、已完成、已取消、退款中
- 用户角色标记：管理员、普通用户、访客、超级权限
- 审核状态标记：待审核、审核通过、审核驳回、审核中
- 数据分类标记：标签归类、属性标注、内容区分、特色标记
三、核心功能体系（严格基于用户定义补全规范）
核心能力：状态标签、纯色标签、边框标签、可关闭、图标标签、尺寸适配、圆角自定义
1. 样式形态分类
- 状态标签：依托全局统一状态色，语义化展示成功、警告、危险、默认、次要状态，与组件库全局色彩体系对齐
- 纯色标签：实心底色+白色文字，高凸显样式，用于重点标记
- 边框标签：透明底色+彩色边框+对应文字色，轻量样式，用于次要标记
2. 图标标签能力
深度适配 AMG-WebUI a-icon 体系，支持标签内置图标展示：
- 支持图标前置展示，适配状态提示、分类标识场景
- 图标尺寸、颜色自动跟随标签尺寸、主题色适配
- 支持自定义业务图标，兼容内置图标/自定义SVG图标
3. 可关闭能力
- 支持开启/关闭关闭按钮，悬浮展示关闭图标
- 关闭事件内置防抖机制，防止快速连续点击触发多次关闭回调
- 关闭动画平滑过渡，移除布局无抖动、无闪烁
- 支持关闭前钩子函数，可拦截关闭、增加二次确认、埋点上报
4. 尺寸适配体系
对齐 AMG-WebUI 全局统一尺寸规范，与 Button、Icon、Link 尺寸体系一致，保障全站视觉统一
支持尺寸：xs / sm / md / lg / xl
不同尺寸对应专属内边距、字号、行高、图标大小、圆角基准值，全部由主题 Token 管控
5. 圆角自定义能力
- 支持全局默认圆角配置
- 支持单组件自定义圆角数值、百分比
- 支持直角、小圆角、标准圆角、大圆角全场景适配
四、色彩规范（用户指定开发标准）
1. 统一状态色规范
严格沿用 AMG-WebUI 全局语义色，与按钮、文字、提示组件状态色完全统一，无色彩割裂
- default：默认常规色
- primary：主题主色
- success：成功/通过/完成
- warning：警告/待处理/提醒
- danger：危险/驳回/失败/异常
2. 自定义色值能力
- 支持自定义底色、边框色、文字色，独立覆盖默认状态色
- 支持十六进制、RGB、主题变量等多种色值传入
- 自定义颜色自动适配深浅对比度，保证文字可读性
3. 主题切换适配
浅色/深色模式自动适配色彩逻辑，状态色、自定义色、边框色、文字色自动联动切换，无固定硬编码颜色
五、交互与状态规范
1. 基础交互状态
常态、hover悬浮状态样式区分，可关闭标签hover高亮关闭图标，交互反馈清晰
2. 禁用状态
支持禁用展示，标签整体置灰、关闭按钮失效、禁止任何交互，用于失效、过期、锁定状态标记
3. 防抖规范
关闭事件内置统一防抖逻辑，防抖时长支持全局配置+单组件自定义，杜绝高频误操作、重复触发事件问题
六、国际化适配规范
完全复用 AMG-WebUI 全局 i18n 能力：
- 标签文案支持全局多语言切换
- 适配长短文案自适应居中、自适应内边距，无文本溢出错乱
- 支持自定义多语言映射文案，适配业务专属状态文本
七、全局配置能力
支持项目全局统一默认配置，一次性规范全站标签样式与行为
- 全局默认尺寸、默认样式形态（纯色/边框）
- 全局默认圆角大小、默认状态色
- 全局关闭事件默认防抖时长
- 全局深色模式标签配色规则
八、工程化与文档要求
- 所有属性、样式、状态、事件全量 TS 枚举约束，代码自动提示、错误拦截
- 提供完整在线 Demo：多形态标签、多状态色、图标标签、可关闭标签、自定义圆角、自定义色值、主题适配
- 配套完整 FAQ、样式适配、事件防抖、主题切换踩坑方案
- 支持按需引入，无冗余代码，适配项目打包优化
九、合规与无障碍细则
- 语义化标签合规，区分展示型/交互型标签
- 色彩对比度满足无障碍阅读标准，状态区分清晰
- 可关闭按钮支持键盘聚焦、回车触发关闭，适配无障碍操作
十、体系对齐说明
本组件所有规范 100%对齐 AMG-WebUI 现有组件体系：主题Token、尺寸体系、状态色、防抖逻辑、i18n、明暗主题、TS类型、全局配置、无障碍规范完全统一，保持整套UI库视觉、交互、代码风格一致性。