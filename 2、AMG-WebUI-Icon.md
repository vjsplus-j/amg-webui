UI组件库 Icon 完整版需求清单（企业级生产可用·独立规范）



先整理现有的icon，把现在使用的与当前需求清单结合，避免使用新清单规范把已存在的icon移除。


一、组件基础规范（全局强制统一）

1. 组件统一前缀：a-icon，与 a-button 等组件库体系命名统一，全局无冲突
2. 技术实现：基于原生 SVG 封装，无第三方图标库依赖、无冗余代码、支持 tree-shaking 按需引入
3. 框架适配：兼容 Vue3 / React，全量 TS 类型约束，无 any 类型，支持完整代码提示
4. 渲染特性：矢量无损渲染，任意缩放无模糊，支持高清屏适配
5. 兼容性：兼容 PC / 移动端，消除渲染差异，支持所有现代浏览器
6. 无障碍规范：内置 a11y 标准，支持 alt 说明、ARIA 标签、屏幕阅读器识别
7. 主题体系：全量颜色、尺寸、动效接入全局主题变量，支持浅色/深色模式自动适配

二、图标库资源规范（业务全覆盖）

1. 内置图标分类（企业级必备全集）

通用基础类：加号、减号、关闭、确认、返回、搜索、刷新、更多、提示、帮助、信息、错误、成功、警告
表单操作类：编辑、删除、新增、保存、重置、上传、下载、导入、导出、清空、复制、粘贴
数据状态类：加载、等待、锁定、解锁、禁用、已选、未选、置顶、下沉
分页导航类：上一页、下一页、首页、尾页、展开、收起、下拉、上拉
文件媒体类：文件夹、文件、图片、视频、音频、链接、预览
权限功能类：用户、角色、权限、设置、菜单、工具箱、批量操作
按钮专属专用类：匹配所有 a-button 业务场景，纯图标按钮、图文按钮全覆盖
2. 图标命名规范
统一短横线命名法，语义清晰、见名知意，全局统一枚举管理，支持 TS 枚举校验，杜绝自定义随意命名
示例：a-icon-add、a-icon-delete、a-icon-refresh、a-icon-upload
三、核心功能能力

1. 调用方式极简

- 支持内置图标名称直接调用，无需手动导入 SVG 文件
- 支持全局统一注册，全项目任意组件直接使用
- 支持按需引入、全量引入两种模式，适配轻量化/大型项目

1. 尺寸适配体系

- 内置5档标准尺寸，与 a-button 尺寸严格一一对应：xs / sm / md / lg / xl
- 支持自定义数值尺寸（px/rem），单组件覆盖全局配置
- 自动适配父组件尺寸，放入按钮、输入框、卡片可自动匹配容器规范

1. 颜色体系能力

- 默认继承父级文字颜色，适配所有主题场景
- 支持自定义单色、十六进制、RGB、主题变量色值
- 支持多色图标、渐变图标自定义配置
- 自动适配状态色：hover、active、禁用、loading、选中态自动变色

1. 动画能力

- 内置旋转动画：适配刷新、同步、加载场景
- 内置闪烁动画：适配消息提醒、状态告警场景
- 支持自定义动画时长、开关控制
- 按钮 loading 状态可接管图标动画，加载结束自动恢复
四、高级拓展能力

1. 自定义图标能力

- 支持传入自定义 SVG 图标、本地图标、远程图标
- 支持第三方图标兼容接入，不污染全局命名空间
- 支持自定义图标裁剪、居中、缩放适配

1. 样式高阶定制

- 支持圆角、边框、透明度自定义
- 支持阴影、hover 悬浮样式单独配置
- 支持旋转角度、翻转（水平/垂直）配置

1. 图文布局适配（对接Button核心能力）

- 支持图标前置、后置、顶部、底部四种布局
- 图标与文字间距全局统一变量控制，支持单组件微调
- 纯图标模式自动居中适配，适配圆形、圆角按钮形态
五、状态体系（全场景闭环）
- 普通状态：默认正常展示
- 禁用状态：自动置灰、降低透明度，跟随按钮禁用状态同步生效
- 加载状态：展示加载动画，覆盖原有业务图标
- hover/active 状态：跟随父组件交互状态自动变色、动效增强
- 选中状态：支持自定义选中色、选中形态
六、全局配置能力

1. 全局默认尺寸、默认颜色、默认间距统一配置
2. 全局动画默认开关、动画时长统一配置
3. 全局深色模式图标色值统一适配规则
4. 支持项目级统一覆盖，无需逐个组件修改

七、工程化与文档规范

1. 所有图标名称 TS 枚举导出，强类型校验
2. 完整 API 文档：属性、事件、配置、插槽、全局变量说明
3. 全覆盖 Demo：基础使用、尺寸搭配、颜色搭配、动画、按钮联动、自定义图标
4. 提供图标检索页、图标预览页、快速复制代码功能
5. 配套 FAQ：渲染异常、尺寸适配、主题失效、动画冲突等问题解决方案

八、联动适配规则（与 a-button 强绑定）

1. 图标尺寸跟随按钮尺寸自动匹配，无需手动传参
2. 按钮 loading 状态自动替换为加载图标，结束恢复业务图标
3. 按钮禁用/只读状态，图标自动同步置灰失效
4. 按钮 hover/active 主题色，图标自动继承同步变色
5. 按钮图文间距、布局位置统一受控，样式零错乱

九、业务覆盖场景
完全覆盖：中后台增删改查、表单操作、弹窗操作、批量处理、数据状态展示、导航菜单、权限按钮、loading加载、状态提示、C端轻交互、多端适配等所有业务图标场景，完全适配自研UI库全组件生态