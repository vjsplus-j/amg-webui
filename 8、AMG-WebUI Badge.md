AMG-WebUI Badge 徽章组件 - 完整规范与需求清单
文档归属：AMG-WebUI 组件库
组件名称：a-badge
适配版本：Vue3 / React 通用
规范等级：企业级生产可用，与 Button / Icon / Link / Typography / Tag / Avatar 体系完全统一
一、基础统一规范（AMG-WebUI 强制标准）
1. 组件命名：统一前缀 a-badge，全局体系统一，无自定义差异化前缀
2. 技术标准：全量 TS 强类型约束、无任意类型、支持 Tree-Shaking 按需引入
3. 兼容性处理：适配 PC / 移动端全场景，统一角标定位、缩放、渲染偏差，杜绝不同端错位、模糊问题
4. 主题体系：尺寸、位置、颜色、圆角、间距全部取自全局主题 Token，支持浅色/深色模式自动切换适配
5. 国际化支持：文字徽章、悬浮提示文案完全兼容全局 i18n，适配多语言展示
6. 样式隔离：自带组件样式隔离，杜绝全局 CSS 污染、样式穿透、父级样式干扰角标定位
7.无障碍规范：遵循 a11y 标准，自带语义计数说明、屏幕阅读器可识别数量与状态
二、组件定位与业务场景
组件定位：轻量状态提示、数量标记组件，用于附属在图标、按钮、头像、卡片、操作入口右上角，实现轻量化消息、数量、状态提醒，不抢占主视觉层级，高辨识度。
核心业务场景（用户指定）：
- 消息未读标记：站内信、会话消息、未读条目数量提醒
- 待办数量提示：待审核、待处理、待跟进事务数量统计
- 全局通知提示：系统通知、公告更新、动态提醒
- 状态标记：新版本、热点、紧急、新增、未读状态标识
三、核心功能体系（严格基于用户定义补全规范）
核心能力：数字角标、红点标记、文字标记、最大值截断、独立颜色、悬浮提示
1. 三种基础标记模式
- 数字角标模式：展示具体数值数量，适用于消息、待办、计数场景
- 红点标记模式：无数字纯小红点，适用于轻量更新、未读提醒、状态红点打点
- 文字标记模式：支持自定义短文本、状态标签，如「NEW」「HOT」「紧急」「新增」等自定义标识
2. 最大值截断能力
- 支持自定义最大阈值，超出阈值自动展示「最大值+」格式，例如 99+、999+
- 数值阈值支持全局默认配置 + 单组件覆盖，适配不同业务计数规范
- 数字超长自动自适应缩放、居中对齐，不挤压变形、不溢出
3. 独立自定义颜色能力
- 支持自定义背景色、文字色，独立于全局状态色，满足业务个性化标记
- 兼容十六进制、RGB、主题变量等色值格式
- 默认继承全局语义色：成功/警告/危险/主要色，与组件库状态体系统一
4. 悬浮提示能力
- 支持鼠标悬浮展示 Tooltip 提示文案，可自定义提示内容（如：未读消息总数、待办明细）
- 气泡样式跟随全局主题，自动适配明暗模式
- 支持延迟显隐，避免误触闪烁
四、开发强制标准（用户指定 100%落地）
1. 数值超出阈值自动展示规则
当绑定数值大于设定最大值时，自动截断展示「max+」，数值小于等于0时自动隐藏角标，避免空数值、0值无效展示，保证页面干净整洁。
2. 红点无数字模式标准
红点模式完全独立样式，无文字、无数字、纯圆点，尺寸固定规范、圆角100%圆形，颜色高亮醒目，适配轻量提醒场景，不冗余展示数量。
3. 位置精准定位标准
角标采用精准相对/绝对定位方案，基于包裹容器自动适配右上角锚点，偏移量全部由主题 Token 统一管控；适配图标、按钮、头像、自定义容器等所有宿主元素，杜绝偏移、错位、贴边、遮挡偏差。
4. 主题色适配标准
默认角标色系自动跟随浅色/深色主题切换，背景色、文字色、红点亮度、透明度全部主题化变量管控，无硬编码色值，主题切换无视觉割裂。
5. 无布局偏移标准
角标使用脱离文档流渲染，不挤占宿主布局、不撑开父容器、不影响原有页面排版，无论是否显示、大小变化，宿主布局零抖动、零偏移。
五、尺寸与视觉体系
对齐 AMG-WebUI 全局五档尺寸体系：xs / sm / md / lg / xl
不同尺寸对应角标直径、字体大小、边距、偏移量、圆角全部主题 Token 统一控制，与 Button、Icon、Avatar 尺寸视觉匹配，搭配不同大小组件无违和感。
六、状态体系规范
- 常态展示：默认标准角标样式，用于常规提醒
- 隐藏状态：数值为0、空值时自动隐藏，也支持手动控制显示/隐藏
- 禁用状态：角标置灰弱化，用于失效、过期、已读提醒
- 闪烁动效：可选开启呼吸/闪烁动画，用于强提醒、紧急通知场景
七、国际化适配规范
- 文字徽章支持多语言文案配置，适配中英日韩俄多语种展示
- 悬浮提示文案、自定义标签文案完全接入全局 i18n
- 多语言文本自动居中适配，无截断、无溢出、无错乱
八、全局配置能力
支持项目全局统一默认配置，一键规范全站角标样式与行为
- 全局默认数值阈值（默认99）
- 全局默认角标尺寸、偏移距离
- 全局默认主题色、红点默认色
- 全局动画默认开关、Tooltip 默认展示规则
- 深色模式角标配色、透明度统一规则
九、工程化与文档要求
- 数值、阈值、颜色、尺寸、位置、文本全部 TS 枚举强类型约束，杜绝错误传参
- 完整 Demo 覆盖：数字角标、红点标记、文字标记、阈值截断、自定义颜色、悬浮提示、主题适配、多尺寸搭配
- 配套完整 FAQ：定位偏移、布局抖动、数值不隐藏、深色模式色差、文本溢出等问题解决方案
- 支持按需引入，零冗余代码，适配打包体积优化
十、合规与无障碍细则
- 自动生成语义化读数，屏幕阅读器可识别「未读数量、状态标签」
- 角标色彩对比度满足无障碍标准，醒目且不刺眼
- 纯装饰角标自动开启 aria-hidden，避免冗余朗读
十一、体系对齐说明
a-badge 所有规范 100%对齐 AMG-WebUI 全套组件体系：Token主题、尺寸档位、状态色值、明暗适配、i18n、TS类型、全局配置、无障碍标准完全统一，保证整套UI库视觉、交互、工程规范高度一致。