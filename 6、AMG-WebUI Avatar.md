AMG-WebUI Avatar 头像组件 - 完整规范与需求清单
文档归属：AMG-WebUI 组件库
组件名称：a-avatar
适配版本：Vue3 / React 通用
规范等级：企业级生产可用，与 Button / Icon / Link / Typography / Tag 体系完全统一
一、基础统一规范（AMG-WebUI 强制标准）
1. 组件命名：统一前缀a-avatar，全局体系统一，无自定义差异化前缀
2. 技术标准：全量 TS 强类型约束、无任意类型、支持 Tree-Shaking 按需引入
3.兼容性处理：适配 PC / 移动端全场景，统一不同端头像裁切、圆角、渲染偏差
4. 主题体系：尺寸、圆角、边框、间距全部取自全局主题 Token，支持浅色/深色模式自动切换适配
5. 国际化支持：文字头像适配多语言字符，兼容各国字符渲染无错乱、无截断
6. 样式隔离：自带组件样式隔离，杜绝全局 CSS 污染、样式穿透覆盖问题
7. 无障碍规范：遵循 a11y 标准，支持语义描述、屏幕阅读器精准识别、聚焦状态合规
二、组件定位与业务场景
组件定位：用户/成员可视化展示组件，用于快速识别主体身份，支持单头像独立展示、多头像堆叠组合展示，视觉轻量化、辨识度高，适配全站人员信息展示场景。
核心业务场景（用户指定）：
- 用户信息展示：个人中心、账号信息、用户资料卡片
- 成员列表展示：后台人员管理、权限成员、参与人员列表
- 聊天场景展示：会话列表、聊天窗口用户头像
- 团队成员展示：团队小组、项目成员、协作人员堆叠展示
三、核心功能体系（严格基于用户定义补全规范）
核心能力：图片头像、文字头像、图标头像、尺寸适配、圆角自定义、边框样式、悬浮提示、头像组堆叠
1. 三类头像渲染模式
- 图片头像：支持本地/网络图片地址，自动居中裁切、等比缩放，保证头像画面居中完整展示，无拉伸变形
- 文字头像：支持用户名、昵称、首字符、多字符展示，自动适配背景色、文字色，文字居中自适应缩放
- 图标头像：深度适配 AMG-WebUI a-icon 体系，默认用户占位图标，支持自定义内置/自定义SVG图标
2. 尺寸适配体系
对齐 AMG-WebUI 全局统一尺寸规范，与所有组件尺寸体系一致，保障全站视觉统一
支持尺寸：xs / sm / md / lg / xl
不同尺寸对应专属宽高、内边距、字号、图标大小、边框粗细，全部由主题 Token 统一管控
3. 圆角与边框能力
- 圆角自定义：支持全局默认圆角配置、单组件自定义数值/百分比圆角，适配直角、小圆角、标准圆角、圆形头像场景
- 边框自定义：支持开启/关闭边框，支持自定义边框色、边框粗细，适配高亮、选中、重点标记场景
- 主题边框适配：浅色/深色模式自动适配默认边框色系，自定义边框色不受主题覆盖
4. 悬浮提示能力
- 支持鼠标悬浮展示 Tooltip 提示，可自定义提示文案（用户名、账号、角色等）
- 悬浮弹窗样式跟随全局主题，适配明暗模式
- 支持提示延迟显示、消失，避免误触闪烁
5. 头像组堆叠能力（核心业务能力）
支持多头像横向堆叠组合展示，适配团队成员、批量人员展示场景
- 堆叠层级规范：从左至右层级依次递减，左侧顶层、右侧底层，堆叠偏移量全局统一 Token 控制
- 数量限制：支持自定义最大展示数量，超出数量自动显示「+N」剩余人数兜底
- 堆叠边框隔离：堆叠头像自动自带白底/主题底色隔离边框，避免头像画面重叠融合、视觉模糊
- 整体适配：头像组整体自适应宽度，兼容不同尺寸头像统一堆叠布局
四、开发标准落地规范（用户指定强制要求）
1. 图片加载失败兜底机制
- 网络图片加载失败、链接失效、图片损坏时，自动降级兜底策略：优先展示文字头像 → 兜底图标头像
- 禁止空白、破碎图片渲染，保证页面布局稳定无塌陷
- 支持自定义失败兜底图片、兜底文案
2. 头像堆叠层级规范
统一固定堆叠 zIndex 层级梯度，从左到右逐级递减，杜绝层级错乱、遮挡反向问题；堆叠偏移距离、边框大小、间距全部纳入主题 Token 统一管控，全局视觉一致。
3. 自定义边框色规范
支持纯色、十六进制、RGB、主题变量自定义边框色，支持静态固定色与主题联动色两种模式，满足普通展示、在线状态、选中状态、VIP 标记等差异化场景。
4. 明暗主题适配规范
头像默认背景、文字色、边框默认色、堆叠隔离底色、悬浮状态样式，全部自动适配浅色/深色主题，无硬编码颜色，主题切换无视觉割裂。
5. 尺寸统一 Token 控制
所有尺寸宽高、字号、图标大小、边框粗细、堆叠偏移量、内边距，全部取自全局主题 Token，支持全局一键覆盖修改，全站统一联动变更。
五、交互与状态规范
1. 基础交互状态
常态、hover悬浮高亮、聚焦状态样式区分，可点击头像鼠标指针变化清晰，交互反馈明确
2. 禁用状态
支持禁用展示，头像整体弱化置灰、禁止点击、禁止悬浮提示，适配离职、失效、冻结用户展示场景
3. 加载状态
图片加载中展示骨架占位效果，加载完成自动替换为头像，加载失败触发兜底机制
六、国际化适配规范
完全复用 AMG-WebUI 全局 i18n 能力：
- 文字头像支持多语言字符正常渲染，兼容中英日韩俄等多语种文字
- 头像组「+N」兜底文案支持多语言切换
- 悬浮提示文案完全适配全局多语言体系
七、全局配置能力
支持项目全局统一默认配置，一次性规范全站头像样式与行为
- 全局默认尺寸、默认圆角形态、默认边框样式
- 全局默认头像背景色、文字色、边框主题色
- 全局头像组默认堆叠偏移量、最大展示数量
- 全局图片加载失败兜底策略优先级
- 全局深色模式头像配色、堆叠隔离色规则
八、工程化与文档要求
- 所有属性、样式、状态、事件、堆叠规则全量 TS 枚举约束，代码自动提示、错误拦截
- 提供完整在线 Demo：图片/文字/图标头像、多尺寸、圆角边框、悬浮提示、头像堆叠组、主题适配、加载兜底
- 配套完整 FAQ：图片加载异常、堆叠层级错乱、主题适配失效、多字符截断等踩坑方案
- 支持按需引入，无冗余代码，适配项目打包优化
九、合规与无障碍细则
- 添加语义化 alt 说明，图片头像无障碍描述合规
- 文字头像对比度满足无障碍阅读标准，清晰可辨
- 可点击头像支持键盘 Tab 聚焦、回车触发交互
- 悬浮提示适配屏幕阅读器读取用户信息
十、体系对齐说明
本组件所有规范 100%对齐 AMG-WebUI 现有组件体系：主题Token、尺寸体系、主题适配、i18n、TS类型、全局配置、无障碍规范完全统一，保持整套UI库视觉、交互、代码风格一致性。