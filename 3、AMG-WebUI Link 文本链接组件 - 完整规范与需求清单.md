# AMG\-WebUI Link 文本链接组件 \- 完整规范与需求清单

**文档归属**：AMG\-WebUI 组件库

**组件名称**：a\-link

**适配版本**：Vue3 / React 通用

**规范等级**：企业级生产可用，与 Button / Icon 体系完全统一

## 一、基础统一规范（AMG\-WebUI 强制标准）

1\. **组件命名**：统一前缀 **a\-link**，全局体系统一，无自定义差异化前缀

2\. **技术标准**：全量 TS 强类型约束、无任意类型、支持 Tree\-Shaking 按需引入

3\. **兼容性处理**：消除移动端 300ms 点击延迟，兼容 PC / 移动端全场景

4\. **主题体系**：完整接入 AMG\-WebUI 全局主题变量，支持浅色/深色模式自动适配

5\. **国际化**：支持全局 i18n 配置、单组件自定义文案，适配多语言项目

6\. **样式隔离**：自带组件样式隔离，杜绝全局 CSS 污染、样式穿透问题

7\. **无障碍规范**：遵循 a11y 标准，支持键盘 Tab 聚焦、回车/空格触发、屏幕阅读器适配

## 二、组件定位与业务场景

**组件定位**：轻量行内交互组件，用于次要操作、文本跳转、内嵌操作场景，替代低权重按钮，保证页面层级视觉克制。

**核心业务场景**：

- 表格行内操作：查看、编辑、详情、复制、日志

- 弹窗/页面小字辅助操作：取消、重置、更多展开

- 文本内嵌协议链接、说明链接、跳转入口

- 权限受控的轻量操作、异步加载跳转操作

- 页面弱引导、次级功能入口

## 三、样式类型体系（与 a\-button 完全对齐）

统一 6 种业务语义类型，全局视觉体系一致，杜绝风格割裂

- **default**：默认文本常规链接

- **primary**：主强调核心跳转链接

- **success**：成功、确认、完成状态链接

- **warning**：警告、提醒、风险提示链接

- **danger**：危险、删除、清空、高危操作链接

- **link**：纯原生超链接风格（默认主色）

## 四、尺寸规范（全局统一五档）

与 Button、Icon 尺寸严格一一对应，字号、行高、间距全局主题统一

可选尺寸：**xs / sm / md / lg / xl**

- xs：超小尺寸（表格紧凑行操作）

- sm：小尺寸（弹窗辅助操作）

- md：默认尺寸（通用场景）

- lg：大尺寸（页面引导链接）

- xl：超大尺寸（ banner、大文本引导）

## 五、下划线规则（Link 核心专属规范）

支持三种下划线模式，适配所有视觉需求，默认遵循系统交互习惯

- **hover（默认）**：常态无下划线，悬浮显示下划线（最符合用户认知）

- **always**：永久显示下划线（协议链接、必填跳转场景）

- **never**：永久隐藏下划线（纯操作按钮式链接）

## 六、完整状态体系（闭环无缺失）

### 1\. 基础交互状态

常态、hover 悬浮、active 点击、focus 聚焦，四态样式完整独立，支持主题自定义

### 2\. 禁用状态

自动置灰、禁止点击、禁止跳转、无 hover 交互、无下划线，区分可交互/不可交互

### 3\. 只读状态

样式正常、禁止点击跳转，仅展示文本，不影响页面布局，区别于禁用态

### 4\. 加载状态（企业级刚需）

支持 loading 开启，加载期间锁定交互、禁止重复点击/跳转，自带防重逻辑，适配异步接口跳转场景

## 七、Icon 图标联动体系（对接 AMG\-WebUI a\-icon）

完全适配项目内置 a\-icon 组件，联动规则与 Button 保持一致

- 支持图标前置、后置双布局

- 图标尺寸跟随 link 尺寸自动适配，无需手动配置

- 图标颜色、hover 状态自动跟随链接主题色变化

- loading 状态优先覆盖自定义图标，加载结束自动还原

- 支持图标间距自定义、全局统一默认间距

## 八、跳转能力（Link 核心功能）

### 1\. 内部路由跳转

支持 path 字符串跳转、route 对象跳转，兼容路由 params / query 参数传递，适配项目路由体系

### 2\. 外部链接跳转

支持 http/https 外链，支持新窗口打开、当前页打开配置

### 3\. 前置拦截能力

提供 beforeClick 前置钩子，支持跳转拦截、权限校验、参数校验、全局埋点、弹窗二次确认

### 4\. 纯事件模式

不传跳转地址时，可作为纯点击操作组件使用，仅触发 onClick 事件，无跳转行为

## 九、业务高阶能力

### 1\. 权限控制系统（与全局组件统一）

支持两种权限模式：权限隐藏（不渲染DOM）、权限置灰（保留布局、禁用交互），支持无权限 hover 提示文案

### 2\. 防重防误触

内置防抖防重机制，可配置防重时长，解决高频快速点击重复跳转、重复请求问题

### 3\. 事件穿透处理

支持阻止冒泡配置，适配表格、弹窗、嵌套组件场景，杜绝事件穿透异常

### 4\. 气泡提示适配

支持绑定 tooltip 提示文案，适配长文本省略、功能说明场景

## 十、全局配置能力

支持项目全局统一默认配置，可一次性覆盖所有 a\-link 默认行为

- 默认尺寸、默认类型

- 默认下划线展示规则

- 默认防重防抖时长

- 默认图标间距、图标布局

- 深色模式默认配色规则

## 十一、工程化与文档要求

- 所有属性、类型、事件、插槽全量 TS 枚举约束，代码自动提示、错误拦截

- 提供完整在线 Demo：基础用法、多类型、多尺寸、图标搭配、loading、权限、跳转拦截

- 配套完整 FAQ、踩坑方案、性能优化说明

- 支持按需引入，无冗余代码，适配项目打包优化

## 十二、合规与无障碍细则

- 自带正确语义 role="link"，适配屏幕阅读器

- 支持自定义 aria\-label 描述，无裸奔元素

- 键盘 Tab 有序聚焦，回车/空格可触发交互，符合无障碍规范

- 禁用态自动关闭聚焦与交互能力

## 十三、体系对齐说明

本组件所有规范**100%对齐 AMG\-WebUI Button、Icon 组件体系**：尺寸、类型、状态、权限、loading、图标联动、工程化标准完全统一，保证整套UI库视觉、交互、代码风格高度一致，无割裂感。

> （注：部分内容可能由 AI 生成）
